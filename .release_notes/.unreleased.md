<!-- To avoid merge conflicts, add items at an arbitrary place in the list. -->

## Breaking Changes

- Drop Python 3.9 support.

## New Features

- Add `list_recursive` method to `StorageClient`.
- Pass-through `s3` parameter to `botocore.config.Config` in S3 storage provider.

## Bug Fixes
