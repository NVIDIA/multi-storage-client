<!-- To avoid merge conflicts, add items at an arbitrary place in the list. -->

- Add `follow_symlinks` parameter to provide control over symbolic link handling during sync.

- Add skip_signature option in GCS storage provider to access public GCS bucket.
