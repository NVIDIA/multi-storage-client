## Bug Fixes

- Match 'file' storage type by equality, not substring.
- Pass `byte_range` object to Rust client `get()` for ranged reads.
- Materialize `StringIO` uploads as bytes before `atomic_write`.
- Set shutdown event before joining diperiodic daemons.
- Adopt `aistore` 1.23 `retry_config` and reader/writer APIs.
