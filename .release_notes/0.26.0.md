- Add support for multipart upload and download for Rust extension.
- Add support for Google Cloud Storage (GCS) for Rust extension.
- Add attribute support for Manifest metadata provider.
- Automatically initialize telemetry provider (for beta observability).
- Add `path` parameter for client list API; rewrite functions to accept only valid file/directory paths instead of partial prefixes.
- Fix the delete API in StorageClient to handle file/directory paths correctly.
- Add `show_attributes` flag for client list API to explicitly fetch detailed object metadata.
- Add `replicas` configuration support for data mirroring across multiple storage backends.
- Add Hydra plugin support for loading configuration files directly from remote storage systems using `msc://` URLs.
- Fix incorrect `tell()` position during file iteration when using `PosixFile`.
